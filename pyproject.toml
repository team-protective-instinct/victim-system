[project]
name = "capstone-victim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["beautifulsoup4>=4.14.3", "requests>=2.33.1"]
[tool.pyright]
venvPath = "."
venv = ".venv"
extraPaths = ["./"]
